import { Link } from "react-router-dom";
import { informationSessionPath } from "./data";
import { useManagedSection } from "@/components/content/ManagedContentProvider";
export default function ContributeFinalCta(){const section=useManagedSection("final_cta",{eyebrow:"Choose your next action",title:"Apply for opportunity—or make opportunity possible.",description:"Applicants can register a professional need. Employers, academics and responsible partners can define the impact they want to fund.",primary_cta_label:"Apply for support",primary_cta_url:informationSessionPath,secondary_cta_label:"Fund an opportunity",secondary_cta_url:informationSessionPath,notice:"IPC funding and sponsorship should remain transparent, consent-based and separate from professional-recognition, judging and editorial decisions."});return <section className="bg-background-50 pb-20 md:pb-28"><div className="container-content"><div className="reveal grid items-end gap-8 bg-accent-700 p-8 text-background-50 md:p-12 lg:grid-cols-[1fr_auto] lg:p-16"><div><span className="eyebrow text-primary-300">{section.eyebrow}</span><h2 className="mt-5 max-w-4xl font-heading text-4xl font-semibold leading-tight md:text-5xl">{section.title}</h2><p className="mt-6 max-w-2xl leading-relaxed text-background-200">{section.description}</p></div><div className="flex flex-col gap-3"><Link to={section.primary_cta_url} state={{enquiry:"Apply for funded support"}} className="btn-primary">{section.primary_cta_label}</Link><Link to={section.secondary_cta_url} state={{enquiry:"Fund an opportunity"}} className="btn-secondary">{section.secondary_cta_label}</Link></div></div><p className="mt-5 text-xs leading-relaxed text-foreground-500">{section.notice}</p></div></section>}
